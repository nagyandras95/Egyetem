package hc.doctor;
import java.util.*;

public class DoctorNominator{
	/*public static Doctor nominate(String name, int regNum) throws RegistrationNumberIsAlreadyTakenException{
		ArrayList<Doctor> doctors = new ArrayList<Doctor>();
		for(Doctor d : doctors ){
			if(regNum == doctors.get(d).getStamp() ){
			throw new RegistrationNumberIsAlreadyTakenException("Already exists!");
			}
		}
		return new Doctor(name, null);
	}
	
	public ResearcherDoctor nominateResearcher (String name, int regNum) throws RegistrationNumberIsAlreadyTakenException {
		ArrayList<Doctor> doctors = new ArrayList<Doctor>();
		for(Doctor d : doctors ){
			if(regNum == doctors.get(d).getStamp() ){
			throw new RegistrationNumberIsAlreadyTakenException("Already exists!");
			}
		}
		ArrayList<ResearcherDoctor> rdoctors = new ArrayList<ResearcherDoctor>();
		for(ResearcherDoctor rd : rdoctors ){
			if(regNum == rdoctors.get(rd).getStamp() ){
			throw new RegistrationNumberIsAlreadyTakenException("Already exists!");
			}
		}
		return new ResearcherDoctor(name, null);*/
	}
}